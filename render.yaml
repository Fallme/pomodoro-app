services:
  - type: web
    name: pomodoro-app
    runtime: node
    plan: free
    buildCommand: npm install --build-from-source
    startCommand: node server.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
