services:
  - type: web
    name: pomodoro-app
    runtime: node
    plan: free
    buildCommand: npm install && npm rebuild better-sqlite3
    startCommand: node server.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
